# ansible-role-traefik — Sync TODO

Generated by ansible-sync-role on 2026-06-06; refreshed 2026-07-07.

The role was already fully synced on 2026-06-06 (see prior entries below,
retained for history). This pass re-checked everything against the current
skill guidance and found four genuine drifts caused by guidance changes
since the last sync, plus verified the rest still holds.

## 2026-07-07 refresh — what changed

### Step 5 — meta/main.yml

**Updated:** removed the `galaxy_info.platforms:` key entirely and folded
the OS/version support statement into `description:` ("Supports Debian
(bookworm, trixie) and Ubuntu (jammy, noble, resolute)."). This reverses
the 2026-06-06 pass, which trimmed `platforms:` rather than removing it —
the skill's guidance changed in the interim because `platforms:` is
confirmed dead weight (Galaxy ignores it) and its `ansible-lint`
`schema[meta]` validator has an unfixable upstream bug that misreports
errors using an unrelated platform's enum. See
`references/known-issues.md#galaxy_infoplatforms-is-dead`.

Re-verified EOL status for all five platform/version pairs against
`scripts/platform-data.json` as of 2026-07-07: jammy, noble, resolute,
bookworm, and trixie are all still supported (none EOL). No versions
dropped. README's Supported Platforms table and `molecule.yml`'s platform
matrix already agreed with this list — no changes needed there.

### Step 7 — preflight assertions

**Updated `tasks/preflight.yml`:** added the missing "ansible-core version
>= 2.20" assertion (Step 7 calls this a universal check every role should
have; the 2026-06-06 pass added `quiet:`/`loop_control:` but missed this
one). Placed first, ahead of the `traefik_sites` type check. Also added a
matching bullet to README's Bootstrap and verification section.

### Step 10 — converge.yml

**Updated:** `molecule/default/converge.yml`'s cache-update `pre_tasks`
used the deprecated top-level `ansible_os_family` fact form instead of
`ansible_facts.os_family`. Fixed both conditions to match
`assets/converge.pre_tasks.yml`. Top-level fact injection is deprecated
in ansible-core and scheduled for removal in 2.24.

### Step 13 — requirements.yml

**Created** `requirements.yml` at the role root (was missing) declaring
`community.docker`, since `tasks/network.yml` and `handlers/main.yml` use
`community.docker.docker_network` / `docker_compose_v2`. Updated the
README's Requirements section to point at the new file instead of just
naming the collection in prose.

### Everything else re-checked, no drift found

Steps 3, 4, 6, 8, 9, 11, 12: re-verified against current guidance —
no `include_vars`/`first_found`, no custom `loop_var`, no OS-family
dispatch, no orphaned task files or templates, `defaults/`/`vars/` split
still correct (only `vars/main.yml` exists, no OS variants), README
Requirements/Supported Platforms/Task Flow sections consistent with
`meta/main.yml` and `molecule.yml`, molecule verifier and platform matrix
unchanged and correct, fixtures still N/A (all converge vars inline,
already anonymized with RFC 5737 CIDRs / AWS example credentials /
`.example.test` FQDNs), testinfra suite still follows conventions.

Note: `scripts/lookup_platform.py` requires Python 3.11+ (`datetime.UTC`);
this session's sandbox only had 3.10. Worked around it by loading
`platform-data.json` directly and computing EOL status by hand rather than
skipping the check — results above are still authoritative for today's
date, but if this comes up again on another role, either run the script
in an environment with 3.11+ or replicate the same manual computation.

---

## 2026-06-06 sync — what was done (retained for history)

### Step 1 — Config files synced from _template

| File | Action |
| :--- | :--- |
| `.ansible-lint` | Skipped (identical) |
| `.gitignore` | **Overwritten** — replaced 144-line generic gitignore with 9-line Ansible-focused template |
| `.pre-commit-config.yaml` | **Overwritten** — bumped ruff-pre-commit from v0.15.14 to v0.15.16 |
| `.yamllint` | Skipped (identical) |
| `ruff.toml` | Skipped (identical) |

(2026-07-07 note: `.gitignore` has since diverged again — role now carries
an alphabetized version plus a `.DS_Store` entry the template lacks. User
chose to keep the role's version this pass rather than re-overwrite.)

### Step 1b — Legacy CI files

No `.travis.yml`, `Jenkinsfile`, or `.circleci/` found. Nothing to remove.
(Still true 2026-07-07.)

### Step 2 — CLAUDE.md

**Kept** — existing CLAUDE.md is a comprehensive, role-specific version
with traefik-specific commit guide extensions (static vs dynamic config
distinction, DNS-01 provider notes, subsystem scopes). Overwriting with
the template would lose that content.
(2026-07-07: kept again — regenerating from the template reproduces this
file almost exactly; the one difference is the manually-added "HTTP-01 /
cross-fire.org — RESOLVED 2026-06-11" note under Open Questions, which
isn't derivable from DESIGN.md and would be lost on overwrite. User chose
to keep as-is.)

### Step 3 — ansible-core 2.20 compliance

No changes needed. Role has no `include_vars` first_found, no custom
loop_var, all sensitive tasks already have `no_log: true`, no OS-family
dispatch logic.

### Step 4 — ansible-lint clean

No changes needed. All file tasks have explicit `mode:`, meta is
complete, no `argument_specs.yml` placeholder content (file doesn't exist
at all — not required by `.ansible-lint`'s enabled rules).

### Step 6 — defaults/ vs vars/

No changes needed. Only `defaults/main.yml` and `vars/main.yml` exist; no
OS-specific variant files to migrate.

### Step 8 — README

**Updated:** added "Supported Platforms" section, expanded preflight
bullet list, added `traefik_cups_enabled` and `traefik_compose_log_driver`
to the variables table.

### Step 9 — molecule.yml platform matrix and verifier

**Updated:** added `p: "no:cacheprovider"` to verifier options; normalized
`directory: ./tests/` → `directory: tests/`. Platform list already
matched `meta/main.yml`.

### Step 11 — Molecule fixtures

Not applicable. This role does not manage user accounts; all test vars
are inline in `converge.yml` with anonymous test data.

### Step 12 — pytest-testinfra test suite

**Updated:** created `molecule/default/tests/__init__.py` with module
docstring; fixed a docstring reference (`playbook.yml` → `converge.yml`)
in `test_default.py`.

### Step 13 — molecule/requirements.txt

**Created** `molecule/requirements.txt` and top-level
`molecule/requirements.txt`-equivalent pins (ansible-lint, molecule,
molecule-plugins[docker], pytest, pytest-testinfra, PyYAML).

---

## Open items / TODO (still open as of 2026-07-07)

### From DESIGN.md open questions

- **Backup of acme-*.json** — out of scope for this role. With wildcard
  certs, losing one file triggers LE rate limits. Decide which
  role/cron job owns the backup.
- **Multi-region rollout order** — decide which region pilots the role.
- **`delay_before_check` tuning** — 120 s is a defensive default. Measure
  actual TXT propagation lag in production and tune down if consistently
  faster.

### From CLAUDE.md implementation order

Molecule scenarios still pending (items 21–24):

- **Item 21** — molecule scenario for preflight failure cases (bad
  resolver reference, uncovered FQDN).
- **Item 22** — molecule scenario for multi-cert split (site FQDNs
  spanning two cert specs).
- **Item 23** — molecule scenario for `traefik_acme_enabled: true` with a
  mock RFC 2136 resolver.
- **Item 24** — expand `molecule/default/tests/test_default.py` to verify
  a live-deployment scenario (container health, network) — the current
  suite deliberately skips this since the default scenario runs with
  `traefik_verify_healthcheck: false` and Docker tasks tagged
  `molecule-notest`.

### From DESIGN.md future work

- Drop the `traefik_sites | length > 0`-style implicit assumption before
  any host reaches all-Docker-label routing (see DESIGN.md's note on this
  — currently no such hard assertion exists, but `traefik_sites` is
  assumed list-shaped throughout).
- External cert source (corp PKI, cert-manager).
- Additional lego DNS providers (Azure DNS, Google Cloud DNS, GoDaddy).
- AWS IAM-via-instance-profile / IRSA for Route53.
- Traefik dashboard / API exposure with authentication middleware.
- Metrics export (Prometheus endpoint, OpenTelemetry tracing).
- Backup automation for `acme-*.json`.

### Upstream notice

The role credits Matthias Leutenegger (`arillso/ansible.traefik` /
`sbaerlocher/ansible.traefik`) in README.md but not in CLAUDE.md. Still
not added — treated as a "ground-up rewrite," not a fork (new public
interface, HTTP-01→DNS-01 replaced), so the template's fork-notice
placeholder was judged not to apply. Revisit only if that framing changes.
