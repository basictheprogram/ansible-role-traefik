# ansible-role-traefik — Sync TODO

Generated by ansible-sync-role on 2026-06-06.

## What was done

### Step 1 — Config files synced from _template

| File | Action |
| :--- | :--- |
| `.ansible-lint` | Skipped (identical) |
| `.gitignore` | **Overwritten** — replaced 144-line generic gitignore with 9-line Ansible-focused template |
| `.pre-commit-config.yaml` | **Overwritten** — bumped ruff-pre-commit from v0.15.14 to v0.15.16 |
| `.yamllint` | Skipped (identical) |
| `ruff.toml` | Skipped (identical) |

### Step 1b — Legacy CI files

No `.travis.yml`, `Jenkinsfile`, or `.circleci/` found. Nothing to remove.

### Step 2 — CLAUDE.md

**Kept** — existing CLAUDE.md is a comprehensive, role-specific version with traefik-specific
commit guide extensions (static vs dynamic config distinction, DNS-01 provider notes,
subsystem scopes). Overwriting with the template would lose that content.

Note: upstream attribution (originally authored by Matthias Leutenegger,
`arillso/ansible.traefik`) is documented in README.md credits but absent from CLAUDE.md.
Consider adding an upstream notice if desired.

### Step 3 — ansible-core 2.20 compliance

No changes needed. Role has no `include_vars` first_found, no custom loop_var, all
sensitive tasks already have `no_log: true`, no OS-family dispatch logic.

### Step 4 — ansible-lint clean

No changes needed. All file tasks have explicit `mode:`, meta is complete, no
argument_specs.yml placeholder content.

### Step 5 — meta/main.yml

**Updated:**
- Added `issue_tracker_url: https://github.com/basictheprogram/ansible-role-traefik/issues`
- Added galaxy_tags: `proxy`, `dns01`, `ubuntu`, `debian`

Platforms confirmed current against platform-eol.md (jammy/noble/resolute, bookworm/trixie).

### Step 6 — defaults/ vs vars/

No changes needed. Only `defaults/main.yml` and `vars/main.yml` exist; no OS-specific
variant files to migrate.

### Step 7 — Preflight assertions

**Updated `tasks/preflight.yml`:**
- Added `quiet: true` to all `assert` tasks
- Added `loop_control: label:` to all three loops:
  - cert resolver check → `label: "{{ item.name }}"`
  - site cert check → `label: "{{ item.name }}"`
  - credential check → `label: "{{ item.key }}"`

### Step 8 — README

**Updated:**
- Added "Supported Platforms" section (Ubuntu 22.04/24.04/26.04, Debian 12/13)
- Expanded preflight bullet list in "Bootstrap and verification"
- Added `traefik_cups_enabled` and `traefik_compose_log_driver` to variables table

### Step 9 — molecule.yml platform matrix and verifier

**Updated:**
- Added `p: "no:cacheprovider"` to verifier options
- Normalized `directory: ./tests/` → `directory: tests/`

Platform list already matched meta/main.yml (debian12, debian13, ubuntu2204, ubuntu2404, ubuntu2604).

### Step 10 — converge.yml

**Updated:**
- Added OS-family-aware apt/dnf cache update `pre_tasks`

All test data was already anonymous and self-contained (RFC 5737 IPs, .example.test domains).

### Step 11 — Molecule fixtures

Not applicable. This role does not manage user accounts; all test vars are inline in
`converge.yml` with anonymous test data.

### Step 12 — pytest-testinfra test suite

**Updated:**
- Created `molecule/default/tests/__init__.py` with module docstring (ruff D104)
- Fixed docstring in `test_default.py`: `playbook.yml` → `converge.yml`

Existing `test_default.py` already follows all required conventions (from __future__,
TYPE_CHECKING guard, `-> None` annotations, no compound assertions) and is comprehensive
for a traefik role (directory checks, permissions, YAML validity, static config, dynamic
config, TLS).

### Step 13 — molecule/requirements.txt

**Created** `molecule/requirements.txt` (new top-level file):
- ansible-lint>=26.4.0
- molecule>=24.0.0
- molecule-plugins[docker]>=23.5.0
- pytest>=8.3.0
- pytest-testinfra>=10.1.0
- PyYAML>=6.0

**Updated** `molecule/default/requirements.txt` to match (was `PyYAML` + `pytest-testinfra` only).

---

## Open items / TODO

### From DESIGN.md open questions

- **Backup of acme-*.json** — out of scope for this role. With wildcard certs, losing one
  file triggers LE rate limits. Decide which role/cron job owns the backup.
- **Multi-region rollout order** — decide which region pilots the role.
- **`delay_before_check` tuning** — 120 s is a defensive default. Measure actual TXT
  propagation lag in production and tune down if consistently faster.

### From CLAUDE.md implementation order

The following molecule scenarios are still pending (items 21–24):

- **Item 21** — molecule scenario for preflight failure cases (bad resolver reference,
  uncovered FQDN).
- **Item 22** — molecule scenario for multi-cert split (site FQDNs spanning two cert specs).
- **Item 23** — molecule scenario for `traefik_acme_enabled: true` with a mock RFC 2136 resolver.
- **Item 24** — expand `molecule/default/tests/test_default.py` — verify dynamic config
  files are rendered, secrets file has mode 0600, container is healthy. (The first two
  are already covered; container health requires a full-deployment scenario.)

### From DESIGN.md future work

- Drop `traefik_sites | length > 0` preflight assertion before any host reaches all-Docker-label routing.
- External cert source (corp PKI, cert-manager).
- Additional lego DNS providers (Azure DNS, Google Cloud DNS, GoDaddy).
- AWS IAM-via-instance-profile / IRSA for Route53.
- Traefik dashboard / API exposure with authentication middleware.
- Metrics export (Prometheus endpoint, OpenTelemetry tracing).
- Backup automation for `acme-*.json`.

### Upstream notice

The role credits Matthias Leutenegger (`arillso/ansible.traefik` /
`sbaerlocher/ansible.traefik`) in README.md but not in CLAUDE.md.
If desired, add an upstream notice at the top of CLAUDE.md:

```
Fork of https://github.com/arillso/ansible.traefik by Matthias Leutenegger.
This fork lives at https://github.com/basictheprogram/ansible-role-traefik.
Bugs and pull requests go to the fork repo, not upstream.
```
